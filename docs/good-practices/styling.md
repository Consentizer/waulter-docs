---
title: Styling & Brand Customization
description: >
  How to apply brand colours consistently, test colour contrast for accessibility,
  handle dark mode, integrate Material Icons, and work with CSS overrides.
status: stub
---

# Styling & Brand Customization

This guide covers best practices for customising the Waulter consent banner's appearance to match your brand while maintaining accessibility and visual consistency.

<!-- TODO:
  ## Applying brand colours consistently
  - Use the `customColorTheme` settings (primary, secondary, background, text)
  - Ensure primary and secondary colours are distinct enough for button hierarchy
  - Test colours against both light and dark backgrounds
  - See: [Dashboard — Styling & Templates](../dashboard/styling.md) for configuration steps

  ## Testing colour contrast for accessibility
  - WCAG 2.1 AA requires 4.5:1 contrast ratio for normal text
  - WCAG 2.1 AA requires 3:1 contrast ratio for large text and UI components
  - Recommended tools for checking contrast:
    - Chrome DevTools colour picker (shows contrast ratio)
    - WebAIM Contrast Checker
    - axe DevTools browser extension
  - Common pitfall: light brand colours on white background fail contrast

  ## Dark mode considerations
  - How Waulter handles `prefers-color-scheme: dark`
  - Setting dark mode colours via customColorTheme
  - Testing both light and dark modes
  - Ensuring contrast ratios pass in both modes

  ## Material Icons integration
  - Waulter uses Material Icons for UI elements (close button, checkboxes, etc.)
  - Icons are bundled with the SDK — no additional loading needed
  - If your site already loads Material Icons, there is no conflict

  ## CSS override tips
  - Waulter banner elements use `.waulter-*` class prefixes
  - Override cautiously: test across all three built-in templates
  - Never override with `!important` unless absolutely necessary
  - Test overrides with the "Vyzkoušet" preview button before going live
  - Common overrides: font size, border radius, spacing

  ## Working with the 3 built-in templates
  - Each template has different layout structure — overrides may apply differently
  - Test your CSS changes across all templates you might use
  - See: [Dashboard — Styling & Templates](../dashboard/styling.md) for template details
-->
