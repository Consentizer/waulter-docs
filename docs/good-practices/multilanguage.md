---
title: Multilanguage Setup
description: >
  How to set up multilanguage consent: language codes, per-language text management,
  testing across languages, and common pitfalls.
status: stub
---

# Multilanguage Setup

This guide covers best practices for setting up and managing consent banners in multiple languages, ensuring all visitors see the banner in their preferred language.

<!-- TODO:
  ## Language codes in configuration
  - Waulter uses ISO 639-1 language codes (e.g. `cs`, `en`, `de`, `fr`, `sk`)
  - How to add supported languages in the dashboard configuration
  - Setting the default/fallback language
  - How the SDK detects the visitor's language (browser `navigator.language`)

  ## Per-language text management in the dashboard
  - Switching between languages in the text editor
  - What needs translation: banner text, purpose names, purpose descriptions, button labels
  - Tips for efficient translation workflows
  - Using the same configuration for all languages vs separate configs per language

  ## Testing across languages
  - How to force a specific language for testing (browser language settings)
  - Using the "Vyzkoušet" preview button with different languages
  - Verifying text fits within the banner layout (some languages are longer)
  - Testing right-to-left (RTL) languages if applicable

  ## Common pitfalls
  - **Missing translations**: what happens when a language is added but texts are not translated
    - Fallback behaviour: which language is shown when translation is missing
  - **Incomplete translations**: some fields translated, others left in default language
  - **Text overflow**: longer translations breaking the banner layout
  - **Language mismatch**: banner language not matching the page language
    - How to synchronise SDK language with page language
  - **Forgetting to test**: always preview each language before going live
-->
